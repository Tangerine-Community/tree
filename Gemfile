source "https://rubygems.org"

gem 'eventmachine' # this is another gem's dependency, but the version it asks for is lame and doesn't build. This one builds.

gem 'sinatra'
gem 'sinatra-contrib', :require => 'sinatra/cookies'
gem 'sinatra-cross_origin', "~> 0.3.1"

gem 'rest-client'
gem 'json'
gem 'logger'
